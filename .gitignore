# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE (optional — remove lines if you want these in the repo)
.vscode/
.idea/

# Logs & env (if you add tooling later)
*.log
.env
.env.*

# Image tooling deps (sharp)
scripts/node_modules/

# Drop-in source for scripts/process-tone-amps.mjs (optional; not committed)
assets/_tone-amps-import.png

# Raw PNGs for scripts/process-feature-images.mjs — run the script to regenerate assets/feat-selling-*.png
assets/_feature-imports/*.png
